import { Flex, Heading } from "@chakra-ui/react";
import React from "react";

export const RequestsAdmin = () => {
  return (
    <Flex direction="column">
      <Heading fontSize="sm">Request Admin</Heading>
    </Flex>
  );
};
